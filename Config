# OS/Z - Copyright (c) 2020 bzt (bztsrc@gitlab), CC-by-nc-sa
# Use is subject to license terms.
# generated by "make config"

# --- common configuration ---
ARCH = x86_64
PLATFORM = ibmpc
DEBUG = 1
OPTIMIZE = 1

# --- disk layout ---
NOINITRD = 0
ESPSIZE = 8256
USRSIZE = 768
VARSIZE = 4
HOMESIZE = 4

# --- build system ---
CFLAGS = -O2 -fno-plt -fvisibility=hidden -ansi -pedantic -Wall -Wextra
include $(dir $(lastword $(MAKEFILE_LIST)))src/core/$(ARCH)/$(PLATFORM)/Makefile.opt
CC=${ARCH}-elf-gcc -Wno-builtin-declaration-mismatch
LD=${ARCH}-elf-ld
